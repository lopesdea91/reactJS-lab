describe("src/@core/controllers/SignOutController/SignOutController.spec.ts", () => {
  let fields = {
    email: "",
    password: "123456",
  };

  it("test onSubmit", async () => {
    const loadingMock = jest.fn();
    const feedbackFormMock = jest.fn();
    const feedbackMock = jest.fn();
  });
});
